<%= form_with(model: [commentable, form_comment], html: { novalidate: true }) do |form| %>
  <% if form.object.errors.any? %>
    <div style="color: red">
      <ul>
        <% form.object.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit t('comments.submit') %>
  </div>
<% end %>
