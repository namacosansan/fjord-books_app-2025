<% title = t('views.common.title_new', name: Book.model_name.human) %>
<% content_for :title, title %>

<h1><%= title %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.back', name: Book.model_name.human), books_path %>
</nav>
