<% content_for :title, t("books.edit.title") %>

<h1><%= t("books.edit.heading") %></h1>

<%= render "form", book: @book %>

<br>

<div>
  <%= link_to t("books.edit.show"), @book %> |
  <%= link_to t("books.edit.back"), books_path %>
</div>
